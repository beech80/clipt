import { AlertStyles } from '../types';

export const defaultAlertStyles: AlertStyles = {
  animation: 'fade',
  duration: 5000,
  soundEnabled: true,
  soundVolume: 0.5,
  fontSize: '24px',
  textColor: '#ffffff',
  backgroundColor: 'rgba(0,0,0,0.8)',
};