import React from 'react';
import { useNavigate, useLocation } from 'react-router-dom';
import '../styles/navigation-bar.css';

const NavigationBar: React.FC = () => {
  // This component is now a placeholder to avoid conflicts with GameBoyControls
  // All navigation is handled by GameBoyControls on the Clipts and SquadsClipts pages
  return null;
};

export default NavigationBar;
